apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: kibana-ingress
  namespace: kube-elk
spec:
  rules:
  - host: buddingdevopsblah.tk
    http:
      paths:
      - backend:
          serviceName: kibana
          servicePort: 5601
